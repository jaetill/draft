{
  "name": "draft",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/build-players.mjs && node scripts/build-owner-profiles.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "probe": "node spike/probe.mjs",
    "build-players": "node scripts/build-players.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
